import os

    def list_directory_contents(path):
        # first we want the function to print all files
        return os.listdir(path)
        #use this function to access files deeper into a decision tree
        return os. walk(path)
        #generate all path files
        return pathlib
        files = [f for f in pathlib.Path().glob("/sys/*.log")]
        print(files)
        # use this function to return a generator.
        return os. scandir()
